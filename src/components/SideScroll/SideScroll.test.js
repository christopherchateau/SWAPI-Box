import React from "react";
import SideScroll from "./index";
import { shallow } from "enzyme";

describe("SideScroll", () => {
  let wrapper;

  beforeEach(() => {
    wrapper = shallow(<SideScroll />);
  });

  it("", () => {

  });
  
});