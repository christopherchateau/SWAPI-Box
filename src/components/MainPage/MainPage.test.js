import React from "react";
import MainPage from "./index";
import { shallow } from "enzyme";

describe("MainPage", () => {
  let wrapper;

  beforeEach(() => {
    wrapper = shallow(<MainPage />);
  });

  it("", () => {

  });
  
});